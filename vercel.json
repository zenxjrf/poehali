{
  "version": 2,
  "buildCommand": "uv pip install -r requirements.txt && cd frontend && npm run build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/$1"
    }
  ]
}
